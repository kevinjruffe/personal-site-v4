---
import { Image } from "astro:assets";
import logo from "../assets/logo.png";
import Footer from "../components/footer";
import Header from "../components/header";

interface Props {
  contentMaxWidth?: string;
  title?: string;
  description?: string;
  noindex?: boolean;
  ogType?: "article" | "website";
  publishedTime?: string;
}

const {
  contentMaxWidth = "65ch",
  title = "KevinRuffe.com",
  description = "Personal website of Kevin Ruffe",
  noindex = false,
  ogType = "website",
  publishedTime,
} = Astro.props;

const canonicalUrl = Astro.url;
const socialImageUrl = new URL(logo.src, Astro.site ?? Astro.url).toString();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, viewport-fit=cover"
    />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl.href} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <meta property="og:site_name" content="KevinRuffe.com" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={canonicalUrl.href} />
    <meta property="og:image" content={socialImageUrl} />
    <meta property="og:image:width" content={logo.width.toString()} />
    <meta property="og:image:height" content={logo.height.toString()} />
    <meta property="og:image:alt" content="KevinRuffe.com logo" />
    {
      publishedTime && (
        <meta property="article:published_time" content={publishedTime} />
      )
    }
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImageUrl} />
    <title>{title}</title>
    <slot name="head" />
  </head>
  <body>
    <div class="site-shell">
      <Header client:load>
        <Image
          src={logo}
          alt="KevinRuffe.com logo"
          width={180}
          height={120}
          class="site-logo"
          priority
        />
      </Header>
      <main
        class="site-content"
        style={`--content-max-width: ${contentMaxWidth};`}
      >
        <slot />
      </main>
      <Footer />
    </div>
  </body>
</html>

<style>
  :global(html) {
    box-sizing: border-box;
    font-size: 112.5%;
  }

  :global(*),
  :global(*::before),
  :global(*::after) {
    box-sizing: inherit;
  }

  :global(body) {
    font-family:
      ui-serif, "Iowan Old Style", "Palatino Linotype", "Book Antiqua",
      Palatino, Baskerville, "Times New Roman", Times, serif;
    margin: 0;
    min-height: 100vh;
    background-color: #f0f0f0;
    line-height: 1.4;
  }

  :global(h1) {
    font-size: 2.5rem;
    line-height: 1.1;
    margin: 0 0 1rem;
  }

  :global(a) {
    color: #215144;
    text-decoration: underline;
    transition:
      color 180ms ease,
      text-decoration-color 180ms ease;
  }

  :global(a:visited) {
    color: #447362;
  }

  :global(a:hover),
  :global(a:focus-visible) {
    color: #16372e;
    text-decoration: none;
  }

  :global(pre) {
    background-color: #f7fbf8;
    border: 1px solid rgba(123, 165, 131, 0.45);
    border-radius: 8px;
    font-family:
      ui-monospace, "SFMono-Regular", SFMono-Regular, Menlo, Monaco, Consolas,
      "Liberation Mono", "Courier New", monospace;
    color: #303030;
    line-height: 1.6;
    max-width: 100%;
    overflow-x: auto;
    padding: 1rem 1.25rem;
    position: relative;
    scrollbar-color: #7ba583 #e7efe9;
    scrollbar-width: thin;
  }

  :global(pre::-webkit-scrollbar) {
    height: 0.7rem;
  }

  :global(pre::-webkit-scrollbar-track) {
    background: #dfeee4;
    border-radius: 999px;
  }

  :global(pre::-webkit-scrollbar-thumb) {
    background: #7ba583;
    border: 2px solid #dfeee4;
    border-radius: 999px;
  }

  :global(.video-embed) {
    aspect-ratio: 16 / 9;
    margin: 1.5rem 0;
    max-width: 100%;
    width: 100%;
  }

  :global(.video-embed iframe) {
    border: 0;
    display: block;
    height: 100%;
    width: 100%;
  }

  @media (max-width: 32rem) {
    :global(pre) {
      padding-top: 2.75rem;
    }

    :global(pre::before) {
      background: rgba(255, 255, 255, 0.92);
      border: 1px solid rgba(123, 165, 131, 0.55);
      border-radius: 999px;
      color: #215144;
      content: "Scroll →";
      font-family:
        ui-serif, "Iowan Old Style", "Palatino Linotype", "Book Antiqua",
        Palatino, Baskerville, "Times New Roman", Times, serif;
      font-size: 0.8rem;
      font-style: italic;
      letter-spacing: 0.02em;
      line-height: 1;
      padding: 0.35rem 0.65rem;
      position: absolute;
      right: 1rem;
      top: 0.75rem;
      white-space: nowrap;
    }
  }

  .site-shell {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  .site-content {
    flex: 1;
    margin: 0 auto;
    max-width: var(--content-max-width);
    padding: 1.5rem max(1rem, env(safe-area-inset-right)) 3rem
      max(1rem, env(safe-area-inset-left));
    width: 100%;
  }

  .site-logo {
    display: block;
    height: auto;
    max-height: 120px;
    width: auto;
  }

  @media (min-width: 48rem) {
    .site-content {
      padding-left: max(1.5rem, env(safe-area-inset-left));
      padding-right: max(1.5rem, env(safe-area-inset-right));
    }
  }
</style>
